{
    "builds": [
        {
            "build": "tsc && node public/app.js",
            "src": "public/app.js",
            "use": "@vercel/static",
            "config": {
                "includeFiles": [
                    "public/**"
                ]
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "public/app.js"
        }
    ]
}